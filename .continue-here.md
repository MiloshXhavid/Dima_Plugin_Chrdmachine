---
phase: v1.7-bug-fixes (standalone session, no GSD phase)
task: build-testing
status: in_progress
last_updated: 2026-03-04T04:06:17Z
---

# Continue Here — v1.7 Bug Fix Session (6 bugs, all coded + built)

## Current State

All 6 bugs coded and built. VST3 installed to `%LOCALAPPDATA%\Programs\Common\VST3` (timestamp 02:44).
Waiting for DAW test results from user. Checklist delivered.

<completed_work>

- Bug 1 ✅ Looper reset syncs random free phases — syncRandomFreePhases() in TriggerSystem, called from PluginProcessor on loopOut.looperReset
- Bug 2 ✅ Population knob = deterministic subdivision shift — default 8→32, offset formula round((pop-32)/32 × 7), SYNC ON uses population offset not spider-motion
- Bug 3 — No code change. User fix: delete `%APPDATA%\REAPER\reaper-vstplugins64.ini` + close/reopen Reaper
- Bug 4 ✅ Left joystick deadzone: smooth rescaling, no S&H — removed S&H from filterX_/filterY_, deadzone rescaling `(abs(v)-dz)/(1-dz)`
- Bug 5 ✅ CC74/CC71/CC12/CC76 on both axes — filterXMode 0-7, filterYMode 0-7, all LFO/gate comparisons updated old 2-5 → new 4-7
- Bug 6a ✅ Arp step-counting fixed — replaced `(ppqPos + beatsThisBlock)/subdivBeats` with `ppqPos/subdivBeats` (start-of-block, avoids estimation drift), added backward-jump guard `curIdx < prev → fire 1 step` for DAW loop/rewind. Both DAW-sync and looper-sync paths in PluginProcessor.cpp ~line 1496-1522
- Bug 6b ✅ Gate length applies to joystick triggers — joystick settle fires randomGateRemaining_ countdown, stillness-close only fires when gateLength==0
</completed_work>

<remaining_work>

- User DAW testing of all 6 bugs (checklist delivered — see previous assistant message)
- If tests pass: commit all changed source files
- If tests fail: fix reported issues
- After all pass: version bump v1.7, build new installer (similar to phase 30 workflow)
</remaining_work>

<decisions_made>

- Bug 6a: chose start-of-block `ppqPos/subdivBeats` over end-of-block `(ppqPos+beatsThisBlock)/subdivBeats`.
  Rationale: end-of-block pre-consumes a boundary one block early → stepsToFire=0 on the block where ppqPos actually crosses it. Start-of-block is simpler and deterministic.
- Bug 6a: backward-jump guard `if (prev < 0 || curIdx < prev) { stepsToFire=1; }` fires 1 immediate step on DAW loop/rewind instead of going permanently silent.
- Install: used `update-local-vst3.ps1` (LOCALAPPDATA, no admin) since `do-install.ps1` targets Program Files which requires elevation.
</decisions_made>

<blockers>

- do-install.ps1 (Program Files) requires admin elevation — use update-local-vst3.ps1 for testing
- COPY_PLUGIN_AFTER_BUILD in CMake also needs admin (fix-install.ps1 exists at project root)
</blockers>

<context>
This session started from clean v1.6 codebase (phases 31-34 had been reset due to bugs).
Previous session had coded bugs 1-6b but bug 6a was only partially fixed (initial fire worked, subsequent steps still broken).
This session fixed bug 6a properly and built+installed.

Files changed (NOT YET COMMITTED):
- Source/GamepadInput.cpp — Bug 4: left stick deadzone smooth rescaling
- Source/PluginProcessor.cpp — Bugs 1,2,5,6a,6b
- Source/PluginProcessor.h — Bug 6a: arpPrevSubdivIdx_ declaration
- Source/TriggerSystem.cpp — Bugs 1,2: syncRandomFreePhases, population offset
- Source/TriggerSystem.h — Bugs 1,2: syncRandomFreePhases declaration
- .planning/ROADMAP.md — may be staged from previous session

Note: old .continue-here.md (2026-03-01, hanging-notes investigation) was overwritten by this file.
That investigation concluded: suspected root cause = re-trigger while pad held overwrites activePitch_[v],
stale noteCount_ entry. That bug is OPEN (not fixed in this session, separate from the 6 v1.7 bugs above).
</context>

<next_action>
Wait for user DAW test feedback. When all 6 pass:
1. `git add Source/GamepadInput.cpp Source/PluginProcessor.cpp Source/PluginProcessor.h Source/TriggerSystem.cpp Source/TriggerSystem.h`
2. Commit: "fix(v1.7): 6 bug fixes — looper sync, population knob, left stick deadzone, CC axis routing, arp step counting, gate length"
3. Plan v1.7 distribution (similar to phase 30)
</next_action>
