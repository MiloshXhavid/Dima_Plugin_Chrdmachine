---
phase: 38.2
slug: custom-cc-routing
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-08
---

# Phase 38.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Catch2 (via CMake FetchContent) |
| **Config file** | `CMakeLists.txt` — `ChordJoystickTests` target |
| **Quick run command** | `cmake --build build --config Release --target ChordJoystickTests && build/Release/ChordJoystickTests.exe` |
| **Full suite command** | Same (all tests in one binary) |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cmake --build build --config Release --target ChordJoystickTests && build/Release/ChordJoystickTests.exe`
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds (build + run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 38.2-01-01 | 01 | 0 | CC-CUSTOM-01,02,03 | unit | `cmake --build build --config Release --target ChordJoystickTests && build/Release/ChordJoystickTests.exe` | ❌ W0 | ⬜ pending |
| 38.2-01-02 | 01 | 1 | CC-CUSTOM-01,02,03 | unit | Same | ✅ | ⬜ pending |
| 38.2-02-01 | 02 | 1 | CC-CUSTOM-04,05 | manual | Build VST3 + smoke test | manual-only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Tests/CustomCcRoutingTests.cpp` — stubs for CC-CUSTOM-01, CC-CUSTOM-02, CC-CUSTOM-03
  - `ccDestToNumber(19, 42)` returns 42
  - `ccDestToNumber(0..18, x)` still returns named CCs (no regression)
  - filterXMode=25 guard returns custom CC, not array OOB

*(Existing `ChordJoystickTests` CMake target — just add new .cpp file and register in CMakeLists.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| UI label appears inline when "Custom CC..." selected | CC-CUSTOM-05 | Requires JUCE GUI runtime | Build + load plugin, select "Custom CC..." in each combo, verify label appears beside combo |
| Preset save/load round-trips custom CC value | CC-CUSTOM-04 | Requires DAW preset save/reload | Set custom CC to 42, save preset, reload, verify combo shows "CC 42" and label shows 42 |
| SWAP button correctly swaps filter custom CC labels | CC-CUSTOM-06 | Requires GUI interaction | Set filterX custom=42, filterY custom=71, toggle SWAP, verify X now routes to 71 and Y to 42 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
