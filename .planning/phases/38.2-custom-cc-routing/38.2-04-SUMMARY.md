---
phase: 38.2-custom-cc-routing
plan: 04
subsystem: ui
tags: [vst3, custom-cc, smoke-test, human-verify]

requires:
  - phase: 38.2-03
    provides: editor UI with inline CC labels, combo wiring, resized(), timerCallback, INV swap

provides:
  - Human-verified custom CC routing across all 4 dropdowns in live DAW

affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - build/ChordJoystick_artefacts/Release/VST3/

key-decisions:
  - "Bug found during smoke-test prep: addAndMakeVisible() overrides setVisible(false) — fixed by switching to addChildComponent() for all 4 custom CC labels (commit a85a51b)"

patterns-established: []

requirements-completed: []

duration: ~10min
completed: 2026-03-08
---

# Phase 38.2-04: Build + Human Smoke Test Summary

**Custom CC routing verified live in DAW — all 5 test scenarios passed, fix applied for labels visible on load**

## Performance

- **Duration:** ~10 min
- **Tasks:** 2
- **Files modified:** 1 (PluginEditor.cpp — bug fix)

## Accomplishments
- VST3 built clean, auto-installed to both VST3 paths
- Bug found and fixed: `addAndMakeVisible()` was overriding `setVisible(false)` on all 4 custom CC labels, causing combos to appear narrowed at load — switched to `addChildComponent()` (commit `a85a51b`)
- Human approval received after fix was deployed

## Task Commits

1. **Task 1: Build and install VST3** — build succeeded, auto-installed
2. **Bug fix: addChildComponent** — `a85a51b` (fix: use addChildComponent for custom CC labels so they start hidden)

## Deviations from Plan

### Auto-fixed Issues

**1. Custom CC labels visible at plugin load**
- **Found during:** Human smoke test observation
- **Issue:** `setVisible(false)` called before `addAndMakeVisible()` — `addAndMakeVisible` internally calls `setVisible(true)`, overriding the hide. All 4 combos appeared narrowed at load even when "None" / named CC was selected.
- **Fix:** Replaced `addAndMakeVisible(label)` with `addChildComponent(label)` for all 4 custom CC labels
- **Files modified:** `Source/PluginEditor.cpp` (4 lines)
- **Verification:** Plugin loads with full-width combos; Custom CC... labels only appear when user selects "Custom CC..." from dropdown
- **Committed in:** `a85a51b`

## Issues Encountered
- None beyond the addChildComponent fix above.

## Next Phase Readiness
- Phase 38.2 complete — custom CC routing fully functional
- Phase 38.3 (Sister LFO attenuation inline slider) is next

---
*Phase: 38.2-custom-cc-routing*
*Completed: 2026-03-08*
