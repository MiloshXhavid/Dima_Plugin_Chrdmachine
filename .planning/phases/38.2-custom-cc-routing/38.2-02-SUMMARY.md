---
phase: 38.2-custom-cc-routing
plan: 02
subsystem: audio
tags: [vst3, juce, apvts, midi-cc, unit-tests, catch2]

# Dependency graph
requires:
  - phase: 38.2-01
    provides: CustomCcRoutingTests.cpp with [cc-routing] tests in TDD RED state

provides:
  - Source/CustomCcRoutingHelpers.h — inline ccDestToNumber(idx, customCcVal) and resolveFilterCcMode(mode, customCcVal)
  - 4 new APVTS AudioParameterFloat params: lfoXCustomCc, lfoYCustomCc, filterXCustomCc, filterYCustomCc
  - "Custom CC..." entry appended to all 4 AudioParameterChoice StringArrays
  - Both kFilterCcNums dispatch sites guarded via resolveFilterCcMode (no OOB array access at index 25)
  - All [cc-routing] unit tests GREEN

affects:
  - 38.2-03 (Wave 2 — UI spinboxes for the 4 custom CC params)
  - PluginEditor.cpp (will need to expose the 4 new params via spinboxes)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - CC dispatch logic extracted to standalone testable header (CustomCcRoutingHelpers.h) with no JUCE dependency
    - Two-argument function signature ccDestToNumber(idx, customCcVal) keeps custom CC value separate from mode index
    - resolveFilterCcMode mirrors ccDestToNumber pattern for filterXMode/filterYMode dispatch

key-files:
  created:
    - Source/CustomCcRoutingHelpers.h
  modified:
    - Source/PluginProcessor.cpp
    - Tests/CustomCcRoutingTests.cpp

key-decisions:
  - "kCcDestTable[] in header is identical to original local t[] array — exact backward compat preserved"
  - "resolveFilterCcMode returns -1 for LFO/Gate target modes (0-6) so callers skip CC output correctly"
  - "filterXCustom/filterYCustom re-read at second dispatch site (LFO direct CC block) because they are in a separate {} scope from the first dispatch site"
  - "Custom CC params default: lfoXCustomCc=74 (Filter Cut), lfoYCustomCc=71 (Resonance), filterXCustomCc=74, filterYCustomCc=71"
  - "BUILD_TESTS=ON must be set when cmake configures — it was OFF so vcxproj lacked CustomCcRoutingTests.cpp (fixed via explicit -DBUILD_TESTS=ON reconfigure)"

patterns-established:
  - "All MIDI CC lookup logic lives in CustomCcRoutingHelpers.h; processBlock calls helper functions rather than inline array access"

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-03-08
---

# Phase 38.2 Plan 02: Custom CC Routing — Backend Summary

**Processor backend for custom CC routing: extracted testable helpers, 4 new APVTS params, guarded kFilterCcNums array access, all Catch2 tests GREEN**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-03-08T18:14:00Z
- **Completed:** 2026-03-08T18:39:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `Source/CustomCcRoutingHelpers.h` — standalone C++17 header with `ccDestToNumber(idx, customCcVal)` and `resolveFilterCcMode(mode, customCcVal)` inline functions, constants `kCustomCcDestIdx=19` and `kCustomFilterModeIdx=25`
- Extended APVTS layout with 4 new `AudioParameterFloat` params (lfoXCustomCc, lfoYCustomCc, filterXCustomCc, filterYCustomCc) and appended "Custom CC..." to all 4 `AudioParameterChoice` StringArrays
- Eliminated all OOB `kFilterCcNums[18]` access risk — both dispatch sites in `processBlock` now call `resolveFilterCcMode()` or `ccDestToNumber()` with the custom CC value
- All 3 `[cc-routing]` Catch2 tests pass GREEN (12 assertions); VST3 build clean

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Source/CustomCcRoutingHelpers.h with extracted helpers** - `f14821a` (feat)
2. **Task 2: Extend APVTS layout and fix all CC dispatch sites in PluginProcessor.cpp** - `6f4875b` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `Source/CustomCcRoutingHelpers.h` — New standalone header: kCcDestTable, kFilterCcNums, kNumLfoTargetModes, kNumFilterCcModes, kCustomCcDestIdx, kCustomFilterModeIdx, ccDestToNumber(), resolveFilterCcMode()
- `Source/PluginProcessor.cpp` — Added #include, 4 APVTS params, "Custom CC..." in 4 StringArrays, removed local ccDestToNumber fn and local kFilterCcNums constants, fixed both dispatch sites
- `Tests/CustomCcRoutingTests.cpp` — Updated comment (minor touch to force MSBuild recompile)

## Decisions Made

- `filterXCustom`/`filterYCustom` are re-read at the second dispatch site (LFO direct CC output block) because the two dispatch sites are in separate `{}` scopes within `processBlock`
- `BUILD_TESTS=ON` explicit reconfigure was needed to regenerate the vcxproj with `CustomCcRoutingTests.cpp` (it had been configured as OFF)
- Default values: `lfoXCustomCc=74` (Filter Cut), `lfoYCustomCc=71` (Resonance) to match the joystick CC defaults; `filterXCustomCc=74`, `filterYCustomCc=71` for symmetry

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Forced CMake reconfigure with BUILD_TESTS=ON**
- **Found during:** Task 1 (verifying [cc-routing] tests)
- **Issue:** `BUILD_TESTS` was `OFF` in CMakeCache.txt so `ChordJoystickTests.vcxproj` lacked `CustomCcRoutingTests.cpp`; tests compiled but the new test file was silently excluded
- **Fix:** Re-ran cmake configure with `-DBUILD_TESTS=ON` to regenerate vcxproj; also trivially edited `CustomCcRoutingTests.cpp` to force MSBuild to recompile the now-present file
- **Files modified:** CMakeCache.txt (via cmake), ChordJoystickTests.vcxproj (auto-regenerated), Tests/CustomCcRoutingTests.cpp (comment update)
- **Verification:** `[cc-routing]` filter now finds 3 test cases; all 12 assertions pass
- **Committed in:** `f14821a` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking)
**Impact on plan:** Necessary infrastructure fix; no scope creep.

## Issues Encountered

- Pre-existing test failure in `LooperEngineTests.cpp` (snapToGrid test, line 435) — `quantizeSubdivToGridSize` returns unexpected values. This failure pre-dates this plan and is unrelated to CC routing changes. Logged to deferred-items.

## Next Phase Readiness

- Backend complete: all 4 custom CC APVTS params registered, both dispatch sites updated, helpers unit-tested
- Wave 2 (Plan 03) can add UI spinboxes to expose the 4 params without touching backend logic
- No blockers

## Self-Check: PASSED

- FOUND: `Source/CustomCcRoutingHelpers.h`
- FOUND: `.planning/phases/38.2-custom-cc-routing/38.2-02-SUMMARY.md`
- FOUND commit `f14821a` (Task 1)
- FOUND commit `6f4875b` (Task 2)

---
*Phase: 38.2-custom-cc-routing*
*Completed: 2026-03-08*
