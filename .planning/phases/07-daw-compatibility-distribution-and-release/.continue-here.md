---
phase: 07-daw-compatibility-distribution-and-release
task: ableton-vst3-category-fix
total_tasks: 1
status: in_progress
last_updated: 2026-02-23T04:30:19.081Z
---

<current_state>
Pre-Phase-07 Ableton debugging session. VST3_CATEGORIES changed from "Fx|MIDI" to "Fx|MIDI Effect" in CMakeLists.txt. New build compiled (05:24, same DLL binary). moduleinfo.json successfully updated in the installed bundle. User ran fix-install.ps1 — moduleinfo.json updated but DLL copy failed (Ableton had the file locked). Session paused before user could confirm whether the rescan + reload worked in Ableton.
</current_state>

<completed_work>

- Diagnosed root cause: Sub Categories were ["Fx","Fx","MIDI"] — Ableton requires "MIDI Effect" not "MIDI" to place plugin in MIDI Effects browser
- Fixed CMakeLists.txt: VST3_CATEGORIES "Fx|MIDI" → "Fx|MIDI Effect"
- Built Release (cmake --build, exit 0 with elevation error on post-build copy — DLL compiled fine)
- Verified new build moduleinfo.json has correct ["Fx","Fx","MIDI Effect"] subcategories
- User ran fix-install.ps1 — moduleinfo.json at C:\Program Files\Common Files\VST3\ChordJoystick.vst3\Contents\Resources\moduleinfo.json successfully updated (timestamp 05:24:19)
- DLL copy blocked (Ableton was running, file locked) — but DLL is the same binary (no code changed), so the old DLL is fine
</completed_work>

<remaining_work>

1. User needs to close Ableton and reopen it
2. Rescan plugins (Preferences → Plug-Ins → Rescan)
3. Verify plugin now appears in MIDI Effects browser section (not just Plug-ins → YourCompany)
4. Drop onto MIDI track device chain → confirm it loads without error
5. If it loads: commit the CMakeLists.txt change with a fix commit
6. If it still fails: investigate further (may need to also check isMidiEffect() C++ return path)
</remaining_work>

<decisions_made>

- VST3 subcategory "MIDI Effect" (not "MIDI") is what Ableton's browser uses to route plugins to the MIDI Effects section. The VST3 SDK constant kFxMidiEffect = "Fx|MIDI Effect".
- DLL did not need replacement — the category is purely in moduleinfo.json, not compiled into the binary. Old DLL (04:59:28) is functionally identical to new build.
- "YourCompany" vendor in moduleinfo.json is cosmetic (PLUGIN_MANUFACTURER_CODE set but COMPANY_NAME not explicitly set) — not causing the load failure, can be fixed later for distribution.
</decisions_made>

<blockers>

- Ableton rescan/load not yet confirmed by user — session paused before they could try
- fix-install.ps1 needs Ableton closed to replace the locked DLL (but DLL replacement wasn't needed this time)
</blockers>

<context>
This was a quick debugging session triggered by the user trying to open the plugin in Ableton after Phase 06 close-out. The plugin was appearing in "Plug-ins → YourCompany" but NOT in MIDI Effects, and failing to load on a MIDI track with an error. Root cause: wrong VST3 subcategory string. Fix is already applied and built. Just needs verification.

If Ableton still refuses to load after the rescan with the new category, the next things to check are:
1. Whether isMidiEffect() is actually returning true at runtime (add a DBG() or check PluginProcessor.h)
2. Whether Ableton 11 vs 12 handles this differently
3. Whether the plugin needs to be on a MIDI track that is set to "No Input" rather than a MIDI instrument track
</context>

<next_action>
Resume: ask user if they closed Ableton, rescanned, and whether ChordJoystick now appears in MIDI Effects. If yes and it loads → commit the CMakeLists.txt change. If no → dig deeper into Ableton's load error message.
</next_action>
