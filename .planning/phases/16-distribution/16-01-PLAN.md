---
phase: 16-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/DimaChordJoystick-Setup.iss
autonomous: false

requirements:
  - DIST-01

must_haves:
  truths:
    - "The installer binary is newer than 2026-02-26 07:34 (post phase 15 commits)"
    - "The installer bundles the correct VST3 bundle: DIMEA CHORD JOYSTICK MK2.vst3"
    - "The user has manually verified LFO modulation and OPTION indicator in a DAW"
  artifacts:
    - path: "installer/DimaChordJoystick-Setup.iss"
      provides: "Updated Inno Setup script for v1.4"
      contains: "AppVersion=1.4"
    - path: "installer/Output/DimaChordJoystickMK2-Setup.exe"
      provides: "Fresh installer binary bundling phase 15 VST3"
  key_links:
    - from: "installer/DimaChordJoystick-Setup.iss"
      to: "build/ChordJoystick_artefacts/Release/VST3/DIMEA CHORD JOYSTICK MK2.vst3"
      via: "Source path in [Files] section"
      pattern: "DIMEA CHORD JOYSTICK MK2\\.vst3"
---

<objective>
Update the Inno Setup script for v1.4, rebuild the installer, and gate on a manual smoke test before publishing.

Purpose: The existing installer (dated Feb 25) predates phase 15 code. Publishing a stale binary would ship users a plugin missing the OPTION indicator and MIDI PC routing. This plan fixes the .iss and produces a fresh binary.
Output: Updated DimaChordJoystick-Setup.iss, freshly compiled DimaChordJoystickMK2-Setup.exe, user smoke-test approval.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-distribution/16-CONTEXT.md
@.planning/phases/16-distribution/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .iss for v1.4 and rebuild installer</name>
  <files>installer/DimaChordJoystick-Setup.iss</files>
  <action>
    Edit `installer/DimaChordJoystick-Setup.iss` to update four things required for v1.4:

    1. Change `AppVersion=1.3` to `AppVersion=1.4`

    2. Replace the `AppComments=` line with:
       `AppComments=v1.4 features: Dual-axis LFO engine (7 waveforms, sync, distortion, level), Beat clock dot on BPM knob, Gamepad Preset Control (Option button + MIDI Program Change)`

    3. In the `[Files]` section, replace the Source and DestDir lines that currently reference `ChordJoystick MK2.vst3` with:
       ```
       Source: "..\build\ChordJoystick_artefacts\Release\VST3\DIMEA CHORD JOYSTICK MK2.vst3\*"; \
           DestDir: "{commoncf64}\VST3\DIMEA CHORD JOYSTICK MK2.vst3"; \
           Flags: ignoreversion recursesubdirs createallsubdirs
       ```
       CRITICAL: The current .iss references `ChordJoystick MK2.vst3` (the v1.3 name). The actual build output is `DIMEA CHORD JOYSTICK MK2.vst3` (CMakeLists PRODUCT_NAME). Using the old path would either fail compilation or bundle the wrong/missing plugin.

    4. In the `[UninstallDelete]` section, change:
       `Type: filesandordirs; Name: "{commoncf64}\VST3\ChordJoystick MK2.vst3"`
       to:
       `Type: filesandordirs; Name: "{commoncf64}\VST3\DIMEA CHORD JOYSTICK MK2.vst3"`

    Also update `DefaultDirName` from `{commoncf64}\VST3\ChordJoystick MK2.vst3` to `{commoncf64}\VST3\DIMEA CHORD JOYSTICK MK2.vst3`.

    After editing the .iss, rebuild the installer using Inno Setup command-line compiler:
    ```bash
    "C:/Program Files (x86)/Inno Setup 6/ISCC.exe" "C:/Users/Milosh Xhavid/get-shit-done/installer/DimaChordJoystick-Setup.iss"
    ```
    If ISCC.exe is not found at that path, try: `ISCC.exe "installer/DimaChordJoystick-Setup.iss"` (if on PATH). If neither works, note the error — the user must open Inno Setup Compil32 GUI and compile manually, then confirm when done.

    After compilation succeeds, verify the output file timestamp:
    ```bash
    ls -la "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimaChordJoystickMK2-Setup.exe"
    ```
    The timestamp MUST be today (2026-02-26) and newer than 07:34.
  </action>
  <verify>
    1. `grep "AppVersion=1.4" installer/DimaChordJoystick-Setup.iss` returns a match
    2. `grep "DIMEA CHORD JOYSTICK MK2" installer/DimaChordJoystick-Setup.iss` returns matches for Source, DestDir, and UninstallDelete lines
    3. `ls -la installer/Output/DimaChordJoystickMK2-Setup.exe` shows today's date (2026-02-26) and timestamp after 07:34
  </verify>
  <done>DimaChordJoystick-Setup.iss updated to v1.4 with correct VST3 bundle name, and installer/Output/DimaChordJoystickMK2-Setup.exe is freshly compiled and timestamped after the phase 15 commits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test — verify all v1.4 features in DAW</name>
  <what-built>Fresh installer built from updated .iss — bundles DIMEA CHORD JOYSTICK MK2.vst3 (phase 15 build). Ready to publish to GitHub.</what-built>
  <how-to-verify>
    1. Install the plugin using the freshly built installer at:
       `C:\Users\Milosh Xhavid\get-shit-done\installer\Output\DimaChordJoystickMK2-Setup.exe`
       (Run as admin; accept SmartScreen "More info" → "Run anyway" if prompted)
    2. Open your DAW and rescan / load the plugin: Dimea Chord Joystick MK2
    3. Confirm LFO modulation is audible on both X and Y axes (enable an LFO, set level > 0, play some notes — pitch should vary with the LFO waveform)
    4. Connect a PS or Xbox gamepad. Press the Option/Guide button. Confirm the plugin UI shows "OPTION" suffix in the gamepad status label.
    5. In preset-scroll mode, press D-pad Up/Down. Confirm MIDI Monitor in your DAW shows Program Change messages (+1 / -1).
    6. Expected outcome: All three features from phases 12-15 work correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if all features work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- installer/DimaChordJoystick-Setup.iss contains AppVersion=1.4 and all DIMEA CHORD JOYSTICK MK2.vst3 references
- installer/Output/DimaChordJoystickMK2-Setup.exe exists and is dated after 2026-02-26 07:34
- User has confirmed LFO, beat clock, and OPTION indicator all function in the DAW
</verification>

<success_criteria>
- .iss updated (AppVersion, AppComments, Source, DestDir, UninstallDelete, DefaultDirName all referencing DIMEA CHORD JOYSTICK MK2)
- Fresh installer binary exists and is dated today, post phase 15
- Smoke test approved by user: LFO audible on both axes, OPTION indicator shows on gamepad toggle
</success_criteria>

<output>
After completion, create `.planning/phases/16-distribution/16-01-SUMMARY.md`
</output>
