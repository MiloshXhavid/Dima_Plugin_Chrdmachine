---
phase: 16-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe
autonomous: true

requirements:
  - DIST-01
  - DIST-02

must_haves:
  truths:
    - "GitHub release v1.4 exists at MiloshXhavid/Dima_Plugin_Chrdmachine with installer binary attached"
    - "Release notes contain LFO, Beat Clock, and Gamepad Preset Control sections plus SmartScreen installation note"
    - "Desktop folder C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/ contains three artifacts: installer, VST3 bundle, source zip"
    - "The v1.4 git tag on the plugin remote points to the current HEAD (all phase 15 docs included)"
  artifacts:
    - path: "installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe"
      provides: "Renamed installer binary for GitHub release asset"
    - path: "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/DimeaChordJoystickMK2-v1.4-Setup.exe"
      provides: "Desktop backup copy of installer"
    - path: "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/source-v1.4.zip"
      provides: "Source archive of repo at v1.4"
  key_links:
    - from: "installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe"
      to: "github.com/MiloshXhavid/Dima_Plugin_Chrdmachine/releases/tag/v1.4"
      via: "gh release create --repo MiloshXhavid/Dima_Plugin_Chrdmachine"
      pattern: "gh release create v1.4"
    - from: "HEAD"
      to: "v1.4 tag on plugin remote"
      via: "git tag -f v1.4 HEAD && git push plugin v1.4 --force"
      pattern: "git tag -f v1.4"
---

<objective>
Retag HEAD as v1.4, create the GitHub release with the renamed installer binary and full release notes, then copy all three artifacts to the versioned desktop backup folder.

Purpose: Phase 16 ships v1.4 publicly. The GitHub release is the deliverable; the desktop backup is the local archive. This plan runs after smoke test approval from plan 01.
Output: GitHub release at MiloshXhavid/Dima_Plugin_Chrdmachine/releases/tag/v1.4 with binary asset; Desktop/Dima_Plug-in/v1.4/ with installer + VST3 bundle + source zip.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-distribution/16-CONTEXT.md
@.planning/phases/16-distribution/16-RESEARCH.md
@.planning/phases/16-distribution/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retag v1.4 to HEAD and rename installer binary</name>
  <files>installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe</files>
  <action>
    Step 1 — Check how far HEAD is ahead of the current v1.4 tag:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.4..HEAD --oneline
    ```
    The plugin remote has v1.4 at ff146af05bac, but HEAD is at 3b4bb79 (includes phase 15 docs). The local tag likely lags.

    If ANY commits are shown (expected), retag v1.4 to HEAD and force-push to the plugin remote:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" tag -f v1.4 HEAD
    git -C "C:/Users/Milosh Xhavid/get-shit-done" push plugin v1.4 --force
    ```
    This ensures the GitHub release points at the commit that includes all phase 12-15 docs, not the older infrastructure commit.

    If the tag is already at HEAD (0 commits ahead), skip the retag — the existing tag is correct.

    Step 2 — Rename the installer binary for the release:
    ```bash
    cp "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimaChordJoystickMK2-Setup.exe" \
       "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe"
    ```
    Note the name difference: original is `Dima` (4 chars), renamed is `Dimea` (5 chars) — per CONTEXT.md locked decision. Do NOT rename in-place; copy and keep the original.

    Verify the copy exists:
    ```bash
    ls -la "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe"
    ```
  </action>
  <verify>
    1. `git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.4..HEAD --oneline` outputs 0 lines (tag is now at HEAD)
    2. `ls "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe"` succeeds
  </verify>
  <done>v1.4 tag points to current HEAD on plugin remote; DimeaChordJoystickMK2-v1.4-Setup.exe exists in installer/Output/.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub release and desktop backup</name>
  <files>
    installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe
    C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/DimeaChordJoystickMK2-v1.4-Setup.exe
    C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/source-v1.4.zip
  </files>
  <action>
    Step 1 — Create the GitHub release with release notes body and the renamed installer as asset.

    Use `--repo MiloshXhavid/Dima_Plugin_Chrdmachine` — the plugin remote, NOT the default origin (which is the GSD framework repo). Omitting --repo would publish to the wrong repository.

    Write the release notes body to a temp file first, then pass via --notes-file:

    Write to `C:/Users/Milosh Xhavid/get-shit-done/installer/Output/release-notes-v1.4.md`:
    ```markdown
    ## Dimea Chord Joystick MK2 v1.4

    Dimea Chord Joystick MK2 v1.4 adds a full dual-axis LFO system, a beat clock indicator, and gamepad preset navigation — the biggest update since launch. All features ship as a single installer with no breaking changes to existing presets.

    ### New in v1.4

    **LFO System**
    - Dual-axis LFO independently controls X and Y joystick modulation
    - 7 waveforms: Sine, Triangle, Saw Up, Saw Down, Square, Sample & Hold, Random
    - Sync mode locks LFO phase to DAW BPM; Free mode runs at any rate (0.01–20 Hz)
    - Per-LFO controls: Phase shift (0–360°), Distortion (additive jitter), Level (depth)

    **Beat Clock**
    - Flashing dot on the Free BPM knob pulses once per beat
    - Follows DAW transport when DAW sync is active; follows Free BPM otherwise

    **Gamepad Preset Control**
    - Option / Guide button toggles preset-scroll mode
    - In preset-scroll mode, D-pad Up/Down send MIDI Program Change +1 / −1
    - Plugin UI shows "OPTION" indicator when preset-scroll mode is active

    ### Install

    1. Download `DimeaChordJoystickMK2-v1.4-Setup.exe`
    2. Run the installer — Windows SmartScreen will show a security warning because this binary is unsigned. Click **More info** → **Run anyway** to proceed.
    3. Admin privileges required (installs to the system VST3 folder)
    4. Rescan plugins in your DAW

    ### Requirements

    - Windows 10/11 64-bit
    - VST3-compatible DAW (Ableton Live, REAPER, etc.)
    - Xbox / PS4 / PS5 controller optional (for gamepad input)
    ```

    Then create the release:
    ```bash
    gh release create v1.4 \
      --repo MiloshXhavid/Dima_Plugin_Chrdmachine \
      --title "ChordJoystick v1.4 — LFO + Beat Clock" \
      --notes-file "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/release-notes-v1.4.md" \
      "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe#DimeaChordJoystickMK2-v1.4-Setup.exe"
    ```
    The `#DimeaChordJoystickMK2-v1.4-Setup.exe` suffix sets the asset display name on the GitHub release page.

    Step 2 — Create the desktop backup folder and copy all three artifacts:
    ```bash
    mkdir -p "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4"

    # Copy renamed installer
    cp "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DimeaChordJoystickMK2-v1.4-Setup.exe" \
       "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/"

    # Copy VST3 bundle (entire directory tree)
    cp -r "C:/Program Files/Common Files/VST3/DIMEA CHORD JOYSTICK MK2.vst3" \
          "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/"
    ```

    Step 3 — Create the source archive from HEAD (not the stale tag, since we just retagged):
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" archive \
      --format=zip \
      --prefix=ChordJoystick-v1.4/ \
      v1.4 \
      -o "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/source-v1.4.zip"
    ```
    (After Task 1 retagged v1.4 to HEAD, archiving v1.4 = archiving HEAD = includes all phase 15 docs.)

    Verify all three backup artifacts exist:
    ```bash
    ls -la "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/"
    ```
    Expected: DimeaChordJoystickMK2-v1.4-Setup.exe, DIMEA CHORD JOYSTICK MK2.vst3/ (directory), source-v1.4.zip
  </action>
  <verify>
    1. `gh release view v1.4 --repo MiloshXhavid/Dima_Plugin_Chrdmachine` shows title "ChordJoystick v1.4 — LFO + Beat Clock" and lists DimeaChordJoystickMK2-v1.4-Setup.exe as an asset
    2. `ls "C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/"` shows all three items: installer exe, VST3 bundle folder, source-v1.4.zip
  </verify>
  <done>GitHub release v1.4 published with installer asset and full release notes. Desktop backup at Dima_Plug-in/v1.4/ contains installer, VST3 bundle, and source zip.</done>
</task>

</tasks>

<verification>
- `gh release view v1.4 --repo MiloshXhavid/Dima_Plugin_Chrdmachine` — release exists, has title and binary asset
- GitHub release notes body contains LFO System, Beat Clock, Gamepad Preset Control sections, and SmartScreen installation note
- `ls C:/Users/Milosh Xhavid/Desktop/Dima_Plug-in/v1.4/` — three artifacts present
- `git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.4..HEAD --oneline` — no output (tag at HEAD)
</verification>

<success_criteria>
- GitHub release tagged v1.4 exists at MiloshXhavid/Dima_Plugin_Chrdmachine with DimeaChordJoystickMK2-v1.4-Setup.exe attached
- Release title is "ChordJoystick v1.4 — LFO + Beat Clock"
- Release notes include: LFO section, Beat Clock section, Gamepad Preset Control section, SmartScreen install note
- Desktop/Dima_Plug-in/v1.4/ contains: DimeaChordJoystickMK2-v1.4-Setup.exe, DIMEA CHORD JOYSTICK MK2.vst3/, source-v1.4.zip
- DIST-01 satisfied: GitHub release with binary and notes
- DIST-02 satisfied: Desktop backup with all three artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/16-distribution/16-02-SUMMARY.md`
</output>
