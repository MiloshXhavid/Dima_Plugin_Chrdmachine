---
phase: 46-mac-build-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 46-03
files_modified:
  - Source/SdlContext.cpp
autonomous: false
requirements:
  - MAC-02
  - MAC-03
  - MAC-04

must_haves:
  truths:
    - "auval -v aumu DCJM MxCJ exits 0 with zero errors and zero warnings"
    - "AU plugin loads in Logic Pro Software Instrument slot without crash and plugin UI is visible"
    - "Reaper (VST3) shows chord MIDI output in MIDI monitor when notes are played"
    - "Ableton Live (VST3) shows chord MIDI output when notes are played"
    - "A PS4 or Xbox controller connected via USB is detected — plugin UI shows controller type label"
  artifacts:
    - path: "~/Library/Audio/Plug-Ins/Components/Arcade Chord Control (BETA-Test).component"
      provides: "Registered AU plugin for auval"
      contains: "x86_64 arm64"
  key_links:
    - from: "auval command"
      to: "AU component in ~/Library/Audio/Plug-Ins/Components/"
      via: "AudioComponentRegistrar scanner"
      pattern: "auval -v aumu DCJM MxCJ"
    - from: "SdlContext::acquire()"
      to: "SDL_Init(SDL_INIT_GAMECONTROLLER)"
      via: "CFRunLoopRunInMode pump if needed"
      pattern: "SDL_NumJoysticks"
---

<objective>
Validate the built plugin via auval (MAC-02), run DAW smoke tests in Logic Pro, Reaper, and Ableton Live (MAC-03), and verify gamepad detection (MAC-04). If the Xbox controller fails to detect, apply the CFRunLoopRunInMode fix to SdlContext.cpp and rebuild.

Purpose: These are the final acceptance gates for Phase 46. auval is automated; DAW smoke tests and gamepad require human verification.
Output: auval pass confirmed by CLI exit code; DAW + gamepad confirmation via human checkpoint.
</objective>

<execution_context>
@/Users/student/.claude/get-shit-done/workflows/execute-plan.md
@/Users/student/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mac-build-foundation/46-03-SUMMARY.md
@.planning/phases/46-mac-build-foundation/46-RESEARCH.md

<interfaces>
<!-- auval commands from research -->

auval invocation:
```bash
# Force AU component registry rescan:
killall -9 AudioComponentRegistrar 2>/dev/null; sleep 2

# Confirm plugin is registered:
auval -a | grep -i "DCJM"

# Run full validation (must exit 0, zero errors, zero warnings):
auval -v aumu DCJM MxCJ
echo "auval exit: $?"
```

Expected auval success output ends with:
```
--------------------------------------------------
AU VALIDATION SUCCEEDED.
--------------------------------------------------
```

SDL2 Xbox run loop fix (apply only if Xbox not detected with no changes):
```cpp
// Add to SdlContext::acquire(), BEFORE SDL_Init, inside #if JUCE_MAC:
#if JUCE_MAC
    CFRunLoopRunInMode(kCFRunLoopDefaultMode, 0.1, false);
#endif
const bool ok = (SDL_Init(SDL_INIT_GAMECONTROLLER) == 0);
```
Add `#include <CoreFoundation/CoreFoundation.h>` at top of SdlContext.cpp if not already present.

Logic Pro smoke test note (from research):
- kAudioUnitType_MusicDevice AU plugins CANNOT show MIDI output in Logic's MIDI monitor — this is a Logic architectural limitation
- Logic success criterion = plugin loads in Software Instrument slot, UI appears, no crash
- MIDI output verification happens only via Reaper and Ableton (VST3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run auval validation — confirm zero errors, zero warnings</name>
  <files></files>
  <action>
Force AU component registry rescan and run the full auval validation suite.

```bash
# Step 1: Force rescan
killall -9 AudioComponentRegistrar 2>/dev/null
sleep 2

# Step 2: Confirm plugin is visible to auval
auval -a | grep -i "DCJM"
```

If "DCJM" does not appear in auval -a output:
- The AU component was not copied. Check: `ls "$HOME/Library/Audio/Plug-Ins/Components/" | grep -i chord`
- If missing, the POST_BUILD command did not run — look at build-mac CMake build log for copy errors
- If present but not listed by auval -a: try `killall coreaudiod` (more aggressive rescan) and wait 5 seconds

```bash
# Step 3: Run full auval
auval -v aumu DCJM MxCJ
echo "auval exit: $?"
```

Expected: "AU VALIDATION SUCCEEDED." and exit code 0.

If auval reports warnings about version hints: check that Plan 02 actually updated all direct layout.add calls. Look for the specific parameter name in the warning. Fix the remaining bare ID in PluginProcessor.cpp, rebuild (repeat Plan 03 Task 1 — only ~5 min since JUCE/SDL2 are cached), then re-run auval.

If auval reports errors (not warnings): document the specific error message in SUMMARY and flag for investigation.
  </action>
  <verify>
    <automated>killall -9 AudioComponentRegistrar 2>/dev/null; sleep 2 && auval -v aumu DCJM MxCJ; echo "auval exit: $?"</automated>
  </verify>
  <done>auval outputs "AU VALIDATION SUCCEEDED." and exits 0. Zero errors and zero warnings reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Universal binary VST3 + AU plugin (arm64+x86_64) built in Release. auval passed with zero errors. Plugin deployed to:
    - ~/Library/Audio/Plug-Ins/VST3/
    - ~/Library/Audio/Plug-Ins/Components/

    Need you to run three DAW smoke tests and one gamepad test to complete Phase 46.
  </what-built>
  <how-to-verify>
    Test 1 — Logic Pro (AU):
    1. Open Logic Pro
    2. Create a new Software Instrument track
    3. In the instrument slot, click and find "Arcade Chord Control (BETA-Test)" under Audio Units > Dimea Arcade
    4. Confirm: plugin loads without error, plugin UI appears (joystick + panels visible), no crash
    5. Play a few MIDI notes into the track — confirm the plugin receives them (joystick responds or you see activity)
    Note: Logic does NOT show MIDI output in its MIDI monitor for AU instrument type — that is expected. Just confirm load + no crash.

    Test 2 — Reaper (VST3):
    1. Open Reaper
    2. Create a new MIDI track
    3. Insert the plugin as VST3 instrument: "Arcade Chord Control (BETA-Test)"
    4. Enable MIDI output from the plugin track
    5. Press keys or use a MIDI keyboard — confirm chord MIDI output appears in Reaper's MIDI monitor

    Test 3 — Ableton Live (VST3):
    1. Open Ableton Live
    2. Add the plugin as an instrument on a MIDI track
    3. Play notes — confirm chord MIDI output is visible

    Test 4 — Gamepad (MAC-04):
    1. Connect a PS4 or Xbox controller via USB
    2. Load the plugin in any DAW (Reaper is fine)
    3. Confirm: controller type label is visible in the plugin UI (e.g., "PlayStation 4" or "Xbox")
    4. Move a joystick axis — confirm the on-screen joystick responds

    If Xbox controller not detected (but PS4 is): that passes MAC-04 (requirement says "PS4 or Xbox"). Document which was tested.
    If neither controller is detected: report back — the SdlContext.cpp run loop fix may be needed.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four tests pass.
    Or describe any failures (e.g., "Logic crashes", "Reaper shows no MIDI output", "no controller detected").
  </resume-signal>
</task>

</tasks>

<verification>
CLI gate (automated):
```bash
auval -v aumu DCJM MxCJ; echo "auval exit: $?"
```

Human gate: All four smoke tests confirmed by user.
</verification>

<success_criteria>
- auval exits 0 with "AU VALIDATION SUCCEEDED." — zero errors, zero warnings
- Logic Pro: AU plugin loads in Software Instrument slot, UI visible, no crash
- Reaper: VST3 plugin emits chord MIDI output to MIDI monitor
- Ableton Live: VST3 plugin emits chord MIDI output
- Gamepad: PS4 or Xbox controller detected via USB — controller type label visible in plugin UI
</success_criteria>

<output>
After completion, create `.planning/phases/46-mac-build-foundation/46-04-SUMMARY.md`
</output>
