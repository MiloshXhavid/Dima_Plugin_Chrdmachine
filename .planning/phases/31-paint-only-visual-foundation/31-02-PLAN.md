---
phase: 31-paint-only-visual-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - Source/PluginEditor.h
  - Source/PluginEditor.cpp
autonomous: false
requirements:
  - VIS-07

must_haves:
  truths:
    - "The cursor glow ring breathes (alpha and radius oscillate) at a rate tied to the beat — one full inhale/exhale per quarter note"
    - "When any gate is open, breathing pauses and glow stays at peak brightness (alpha 0.45f, radius dotR+12)"
    - "Beat sync: PluginEditor::timerCallback() calls joystickPad_.resetGlowPhase() on beat; JoystickPad never reads beatOccurred_ directly"
    - "glowPhase_ advances at 1/60 per timerCallback() tick (60 Hz) when no gate is open and no beat reset"
    - "Visual result confirmed by human: glow ring pulses visibly in time with the beat dot on the Free BPM knob"
  artifacts:
    - path: "Source/PluginEditor.h"
      provides: "beatPulse_ member present (already exists); joystickPad_ member with public resetGlowPhase() accessible"
    - path: "Source/PluginEditor.cpp"
      provides: "JoystickPad::timerCallback() extended with glowPhase_ advancement; PluginEditor::timerCallback() calls joystickPad_.resetGlowPhase() after beat exchange"
  key_links:
    - from: "PluginEditor::timerCallback()"
      to: "joystickPad_.resetGlowPhase()"
      via: "called immediately after proc_.beatOccurred_.exchange(false) returns true"
      pattern: "joystickPad_.resetGlowPhase()"
    - from: "JoystickPad::timerCallback()"
      to: "glowPhase_"
      via: "advance by 1.0f/60.0f each tick; lock at 1.0f when anyGateOpen; fmod wrap"
      pattern: "glowPhase_ = std::fmod"
---

<objective>
Add beat-synced breathing animation to the cursor glow ring by wiring glowPhase_ advancement in JoystickPad::timerCallback() and resetGlowPhase() calls in PluginEditor::timerCallback(). Concludes with a human visual checkpoint.

Purpose: Completes the Phase 31 visual foundation — the glow ring now provides real-time tempo feedback, tying the visual identity to the music's pulse.

Output: glowPhase_ is driven at 60 Hz, pauses on gate-open, resets on beat. Human confirms visual correctness.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-paint-only-visual-foundation/31-CONTEXT.md
@.planning/phases/31-paint-only-visual-foundation/31-01-SUMMARY.md

<interfaces>
<!-- beatOccurred_ exchange pattern — PluginEditor.cpp lines 4368-4374 (target insertion point). -->
// The beat exchange block in PluginEditor::timerCallback():
//
//   if (proc_.beatOccurred_.exchange(false, std::memory_order_relaxed))
//       beatPulse_ = 1.0f;
//   else if (beatPulse_ > 0.0f)
//       beatPulse_ = juce::jmax(0.0f, beatPulse_ - 0.11f);
//
// Add joystickPad_.resetGlowPhase() call immediately inside the `if` block,
// AFTER beatPulse_ = 1.0f:
//
//   if (proc_.beatOccurred_.exchange(false, std::memory_order_relaxed))
//   {
//       beatPulse_ = 1.0f;
//       joystickPad_.resetGlowPhase();   // <-- ADD THIS LINE
//   }
//
// CRITICAL: Do NOT add a second exchange call. beatOccurred_ is already cleared here.
// JoystickPad::timerCallback() must NOT read beatOccurred_ at all.

<!-- isGateOpen() pattern — already in JoystickPad::timerCallback(). -->
// PluginEditor.cpp line 894 — existing call:
//   if (!proc_.isGateOpen(v)) continue;
// The glowPhase_ update uses the same proc_.isGateOpen(v) pattern.

<!-- JoystickPad::timerCallback() end — insertion point for glowPhase_ block. -->
// Current timerCallback() ends at line 914 with repaint().
// Add the glowPhase_ block BEFORE repaint():
//
//   // ── Cursor breathing animation ────────────────────────────────────────────
//   {
//       bool anyGateOpen = false;
//       for (int v = 0; v < 4; ++v)
//           anyGateOpen |= proc_.isGateOpen(v);
//
//       if (anyGateOpen)
//           glowPhase_ = 1.0f;  // locked to full brightness while gate held
//       else
//           glowPhase_ = std::fmod(glowPhase_ + 1.0f / 60.0f, 1.0f);
//   }
//
//   repaint();
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend JoystickPad::timerCallback() with glowPhase_ advancement</name>
  <files>Source/PluginEditor.cpp</files>
  <action>
In `JoystickPad::timerCallback()` (currently ending around line 914), add a `glowPhase_` update block immediately BEFORE the final `repaint()` call.

The block checks anyGateOpen across all 4 voices (same pattern already used at line 894 for particle spawning). When any gate is open, glowPhase_ is locked at 1.0f (peak glow). When no gate is open, glowPhase_ advances by 1.0f/60.0f per tick and wraps with fmod.

Insert:
```cpp
    // ── Cursor breathing animation ────────────────────────────────────────────
    // glowPhase_ (0..1) drives the breathing glow ring in paint().
    // Beat sync: resetGlowPhase() is called by PluginEditor::timerCallback() on beat.
    // Gate-open: hold at peak brightness (full inhale). Resume on release.
    {
        bool anyGateOpen = false;
        for (int v = 0; v < 4; ++v)
            anyGateOpen |= proc_.isGateOpen(v);

        if (anyGateOpen)
            glowPhase_ = 1.0f;  // locked to full glow while a gate is open
        else
            glowPhase_ = std::fmod(glowPhase_ + 1.0f / 60.0f, 1.0f);
    }

    repaint();
```

(Remove or replace the existing bare `repaint();` line that was there before — do not duplicate it.)
  </action>
  <verify>
    <automated>grep -n "glowPhase_" "Source/PluginEditor.cpp" && grep -n "anyGateOpen" "Source/PluginEditor.cpp"</automated>
  </verify>
  <done>glowPhase_ advancement block is present in JoystickPad::timerCallback(). anyGateOpen loop over 4 voices confirmed. Build still compiles with 0 errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire resetGlowPhase() into PluginEditor::timerCallback() beat exchange</name>
  <files>Source/PluginEditor.cpp</files>
  <action>
In `PluginEditor::timerCallback()`, find the beat pulse exchange block (around line 4371):

```cpp
    if (proc_.beatOccurred_.exchange(false, std::memory_order_relaxed))
        beatPulse_ = 1.0f;
    else if (beatPulse_ > 0.0f)
        beatPulse_ = juce::jmax(0.0f, beatPulse_ - 0.11f);
```

Change it to use braces on the `if` arm and add the resetGlowPhase() call:

```cpp
    if (proc_.beatOccurred_.exchange(false, std::memory_order_relaxed))
    {
        beatPulse_ = 1.0f;
        joystickPad_.resetGlowPhase();  // sync cursor breathing to beat
    }
    else if (beatPulse_ > 0.0f)
    {
        beatPulse_ = juce::jmax(0.0f, beatPulse_ - 0.11f);
    }
```

CRITICAL rules:
- Do NOT add a second `.exchange()` call on beatOccurred_. The single existing exchange clears the flag; resetGlowPhase() simply resets the float.
- Do NOT move the exchange call.
- The `else if` branch is unchanged in behavior.
- This is a minimal, targeted edit — change only these ~6 lines.
  </action>
  <verify>
    <automated>grep -n "resetGlowPhase" "Source/PluginEditor.cpp"</automated>
  </verify>
  <done>resetGlowPhase() call is present inside the beatOccurred_ exchange if-block in PluginEditor::timerCallback(). Only one occurrence of resetGlowPhase() in the file (the implementation in JoystickPad::resetGlowPhase and this call site). Build compiles with 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 31 visual foundation:
- Near-black 0xff05050f background replacing the old Clr::accent fill
- Milky way band image baked in resized(), opacity driven by randomDensity (1=15%, 8=100%)
- 300-star starfield, star count driven by randomPopulation (1=0 stars, 64=300 stars)
- Blue-to-magenta heatmap radial gradient, circle-clipped, drawn over the milky way
- Semitone grid: horizontal dashed lines (joystickYAtten count), solid vertical lines (joystickXAtten count), in-scale lines brighter than out-of-scale
- Border stroke alpha 0.30f (was 0.5f)
- Beat-synced breathing glow ring: pulses at the beat rate, holds at full brightness when any gate is open
  </what-built>
  <how-to-verify>
1. Build and install:
   ```
   cd "C:/Users/Milosh Xhavid/get-shit-done/build" && cmake --build . --config Release --target ChordJoystick_VST3
   ```
   Then run fix-install.ps1 or copy the VST3 manually.

2. Load the plugin in Ableton Live or pluginval.

3. Verify background layers:
   - Joystick pad background is near-black (very dark navy/black) — NOT the old teal/blue Clr::accent color.
   - A faint diagonal milky way band is visible (lighter diagonal streak across the pad).
   - Turn randomDensity knob from 1 to 8: the milky way band should become progressively brighter (barely visible at 1, fully visible at 8).

4. Verify starfield:
   - Set randomPopulation to 1: no stars (or very few) visible.
   - Set randomPopulation to 32 (~50%): roughly 150 stars visible — tiny dots of varying sizes, mostly white with a few blue-tinted and warm yellow.
   - Set randomPopulation to 64: all 300 stars visible, dense starfield.

5. Verify heatmap:
   - The pad has a subtle blue-to-magenta radial gradient visible through the space background — blue at the top, magenta at the lower right, transparent at center, colored at the circle edge.
   - The gradient is clipped to the inscribed circle — no color in the square corners.

6. Verify grid:
   - Set joystickXAtten and joystickYAtten both to 24 (default): approximately 24 vertical solid lines and 24 horizontal dashed lines visible.
   - Horizontal lines should show a clear dash pattern (on/off segments), vertical lines should be solid.
   - Horizontal lines appear slightly brighter than vertical lines.
   - Changing the current scale (e.g., toggle between Major and Chromatic) should make more or fewer lines appear bright — in-scale lines are brighter (0.28-0.38f alpha), out-of-scale are dimmer (0.10-0.14f alpha).

7. Verify cursor:
   - The cursor dot has a dark halo (dark circle behind it dimming the background).
   - A cyan glow ring around the cursor breathes: it grows and shrinks rhythmically in sync with the beat dot on the Free BPM knob. One full pulse per beat.
   - Hold any touchpad: while the gate is open, the glow ring stays at full brightness without breathing.
   - Release the touchpad: breathing resumes.

8. Verify border:
   - The rectangular border around the joystick pad is visibly less bright than before (0.30f alpha vs old 0.5f).

Expected: All 8 checks pass. The pad looks like a space scene with a musical overlay.
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe specific issues observed (e.g., "stars not appearing", "grid not dashed", "glow not pulsing").</resume-signal>
</task>

</tasks>

<verification>
Build verification (run before checkpoint):
```
cd "C:/Users/Milosh Xhavid/get-shit-done/build" && cmake --build . --config Debug --target ChordJoystick_VST3 2>&1 | grep -E "error|warning" | head -20
```
Expected: 0 errors.

Atomic ownership sanity check:
```
grep -n "beatOccurred_" "Source/PluginEditor.cpp" | grep -v "//\|PluginEditor::timerCallback\|beatPulse_"
```
Expected: The only exchange call is in PluginEditor::timerCallback(). JoystickPad::timerCallback() should have NO reference to beatOccurred_.

```
grep -n "beatOccurred_" "Source/PluginEditor.cpp"
```
JoystickPad::timerCallback() lines should NOT appear in this output (the function spans approximately lines 827-915).
</verification>

<success_criteria>
- glowPhase_ advances at 1/60 per timerCallback() tick when no gate is open
- anyGateOpen check locks glowPhase_ at 1.0f (peak glow) while any voice gate is held
- PluginEditor::timerCallback() calls joystickPad_.resetGlowPhase() inside the beatOccurred_ exchange if-block — a single targeted line addition
- JoystickPad::timerCallback() has NO read or exchange of beatOccurred_
- Build compiles 0 errors in both Debug and Release
- Human checkpoint approved: all 8 visual tests pass
- No audio/MIDI behavior changed
</success_criteria>

<output>
After human checkpoint approval, create `.planning/phases/31-paint-only-visual-foundation/31-02-SUMMARY.md` with:
- glowPhase_ advancement pattern and gate-pause logic
- resetGlowPhase() wiring approach (why PluginEditor owns the beat, not JoystickPad)
- Any visual issues discovered during checkpoint and how they were resolved
- Exact line numbers for the glowPhase_ block in timerCallback() and the resetGlowPhase() call in PluginEditor::timerCallback()
</output>
