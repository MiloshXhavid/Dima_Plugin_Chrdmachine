---
phase: adhoc-v1.7-bug-fixes
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-04T12:07:45Z
---

<current_state>
Arp gate length fix applied and built+installed. DAW closed, ready to test.
Fix-install.ps1 also updated to use correct plugin name.
</current_state>

<completed_work>

## Session 1 (earlier today) — 6-bug fix batch built+installed
- Bug 1: Looper reset syncs random free phases
- Bug 2: Population knob = deterministic subdivision shift (default 32, offset ±7)
- Bug 4: Left joystick deadzone smooth rescaling, no S&H
- Bug 5: CC74/CC71/CC12/CC76 available on both X and Y axis (8 modes each)
- Bug 6a: Arp step-counting fixed (start-of-block index, backward-jump guard)
- Bug 6b: Gate length applies to joystick triggers

## Session 2 (this session) — arp fixes + gate length bug
- Arp start behavior fix (committed in previous session, included in today's build)
- Gate length fix (committed in previous session, included in today's build)
- **Arp gate length minimum bug fixed**: `gateBeats = subdivBeats * std::max(gateRatio, 0.02)`
  - Root cause: gateRatio=0.0 produced gateBeats=0 → no gate-timer → note lasted full subdivision
  - Fix: minimum 2% of subdivision so slider fully closed = very short/staccato notes
- fix-install.ps1 updated: old name "Chord Joystick Controller (BETA)" → "Joystick Chord Control (β-test)"
</completed_work>

<remaining_work>
1. Test in DAW:
   - Gate length: move slider fully closed → arp notes should be very short/staccato
   - Gate length: move slider fully open → arp notes should be full subdivision length
   - Confirm CC74=cutoff / CC71=resonance mapping matches user's synth
2. Commit changes once tested
</remaining_work>

<decisions_made>
- Gate minimum set to 2% of subdivision (not 0 = manual mode) — user wants staccato at minimum, legato behavior removed
- CC74 = Filter Cutoff, CC71 = Resonance (MIDI standard) — if user's synth uses different mapping, they swap the dropdown
- fix-install.ps1 plugin name corrected to match current CMake PRODUCT_NAME
</decisions_made>

<blockers>
- None. Awaiting DAW test.
- Open question: which synth is the user using? CC71/CC74 confusion may be synth-side (non-standard mapping).
</blockers>

<context>
User reported two issues:
1. CC71 felt like it was modulating the filter (cutoff) instead of resonance — code is correct per MIDI standard, likely synth-side mapping
2. Gate length slider had no effect on arp note length — root cause was gateRatio=0 bypassing the gate timer entirely

Both investigated from source. Fix 2 is applied and built. Fix 1 needs synth identification to confirm.
</context>

<next_action>
Open DAW, load plugin, test arp gate length slider (fully closed = staccato, fully open = legato/full subdivision).
Then ask user which synth they're using to resolve the CC71/CC74 confusion.
</next_action>
