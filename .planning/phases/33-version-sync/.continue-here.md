---
phase: adhoc-version-sync
task: 1
total_tasks: 1
status: in_progress
last_updated: 2026-03-04
---

<current_state>
Mid-session bug fix + version sync work. Not inside a formal GSD phase.
User asked to wire up version so CMakeLists.txt is the single source of truth
and the Inno Setup .iss installer file auto-syncs from it.
Next action: read installer/DimaChordJoystick-Setup.iss, then use CMake
configure_file to generate it from a .iss.in template.
</current_state>

<completed_work>

## Bug fixes applied and built (PluginProcessor.cpp + PluginEditor.cpp)

### Arp free-run fix
- `arpClockOn` changed from `looper_.isPlaying() || isDawPlaying` → `true` when !arpSyncOn
- Arm-wait cleared immediately when no external clock: `!arpOn || (!arpSyncOn && !looper_.isPlaying() && !isDawPlaying)`
- Arp now free-runs from the Free Tempo knob without requiring DAW transport or looper

### Arp hanging note fix
- Cleanup on arp stop now sends note-off unconditionally (bypasses noteCount gate)
- noteCount still decremented for accuracy

### PluginEditor.cpp dropdown fix (the real "old version" bug)
- filterXModeBox_ updated from 6 hardcoded items → 8 items matching new APVTS:
  0=Cutoff(CC74), 1=VCF LFO(CC12), 2=Resonance(CC71), 3=LFO Rate(CC76),
  4=LFO-X Freq, 5=LFO-X Phase, 6=LFO-X Level, 7=Gate Length
- filterYModeBox_ updated similarly:
  0=Resonance(CC71), 1=LFO Rate(CC76), 2=Cutoff(CC74), 3=VCF LFO(CC12),
  4=LFO-Y Freq, 5=LFO-Y Phase, 6=LFO-Y Level, 7=Gate Length
- All index checks in timerCallback updated: old 2-5 → new 4-7
- onChange MOD FIX reset threshold: >= 2 → >= 4
- updateMod calls: xMode 2→4, 3→5, 4→6; yMode same
- Label/suffix checks: same shift

### Version bumped
- CMakeLists.txt: VERSION 1.5.0 → 1.7.0
- RC file manually patched: FILEVERSION 1,5,0,0 → 1,7,0,0, strings → "1.7.0"
- Built + installed to C:\Program Files\Common Files\VST3\ (Mar 4 11:57)
</completed_work>

<remaining_work>

## Version sync automation (user's last request)
Wire up CMakeLists.txt VERSION as single source of truth for both:
1. The Windows RC file (already handled manually this session — consider automating)
2. The Inno Setup .iss installer file (NOT YET DONE)

Plan:
- Read installer/DimaChordJoystick-Setup.iss to find AppVersion line
- Create installer/DimaChordJoystick-Setup.iss.in template with @PROJECT_VERSION@ placeholder
- Add configure_file() call to CMakeLists.txt to generate the .iss from .in
- Add installer/*.iss to .gitignore (generated file)
- Commit

Then to release: only change VERSION in CMakeLists.txt → both installer and RC version stay in sync.
</remaining_work>

<decisions_made>

- Single source of truth = CMakeLists.txt `project(ChordJoystick VERSION x.y.z)`
- RC file gotcha: JUCE doesn't regenerate it — must patch manually or add cmake custom command
- The "old dropdown" bug was a CODE bug (PluginEditor not updated), not a DAW cache issue
- Ableton Live 11 rescans VST3 when file timestamp changes — no manual cache deletion needed
  IF the file is actually updated (needs Ableton closed during copy)
</decisions_made>

<blockers>
None.
</blockers>

<context>
This was an ad-hoc bug-fix session (not a formal GSD phase). All v1.7 bug fixes
are now applied and built. The version sync work (CMakeLists → .iss) is the one
outstanding task the user requested but context ran out before we could read the .iss file.

The .iss file is at: installer/DimaChordJoystick-Setup.iss
The approach is standard CMake configure_file with a .iss.in template.
</context>

<next_action>
1. Read installer/DimaChordJoystick-Setup.iss
2. Create installer/DimaChordJoystick-Setup.iss.in with @PROJECT_VERSION@ replacing the version string
3. Add configure_file(installer/DimaChordJoystick-Setup.iss.in installer/DimaChordJoystick-Setup.iss) to CMakeLists.txt
4. Test by changing VERSION and reconfiguring — verify .iss updates automatically
5. Commit
</next_action>
